# Security Standards

- Never hardcode secrets, credentials, or API keys in any file
- Use environment variables or secret managers for all sensitive values
- Apply least-privilege to all IAM roles and policies
- Encrypt data at rest and in transit
- Enable audit logging for all production systems
- Scan dependencies for CVEs before deploying
- Validate all inputs at system boundaries
- Review security implications of every infrastructure change
